# mssql-sampdledb-linux
Simple Docker image to mount a database from a shared volume and run install scripts.

**Note**
Before running make sure the line terminators for entrypoint.sh are LF only for Linux.
